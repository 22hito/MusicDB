import React from 'react';
import { SettingsScreen } from '@/screens/SettingsScreen';

// Вкладка "Налаштування" — як на мобільному сайті (профіль — аватаром у шапці).
export default function SettingsTab() {
  return <SettingsScreen />;
}
